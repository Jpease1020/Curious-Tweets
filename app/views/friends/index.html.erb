<div class="container col-sm-12 col-md-12 col-lg-12">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h1 class="panel-title text-center">Who I Follow</h1> <%= link_to "Dashboard", dashboard_path %>
    </div>
    <div class="panel-body">
      <div class="row">
        <% @friends.each do |friend| %>
        <div class="well col-sm-12 col-md-12 col-lg-12">
          <div class="panel panel-default">
            <div class="panel-heading">
              <h1 class="panel-title friend-title"><%= friend["screen_name"] %></h1>
            </div>
            <div class="panel-body friends-panel">
              <ul class="friends">
                <li><%= image_tag friend.profile_image_url.to_s.sub("_normal.","."), size: "50x50" %></li>
                <li>Name: <%= friend['name'] %></li>
                <li>Description: <%= friend['description'] %></li>
                <li>Last Tweet: <%= friend["status"]["text"] %></li>
                <div class="col-sm-2 col-md-2 col-lg-2 unfollow">
                <li><%= button_to "Unfollow", friend_path(friend["id"]), method: :delete, class: "btn btn-primary" %></li>
              </div>
              </ul>
            </div>
          </div>
        </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
